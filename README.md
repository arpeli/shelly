# shelly

A simple, love-themed static site to archive poems and letters for Michelle ("Shelly").

## Run locally

From `/home/runner/work/shelly/shelly`:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000` in your browser.
